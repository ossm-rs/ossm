name: Create Release

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    name: Build and release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bump version and create tag
        id: tag
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ github.token }}

      - uses: esp-rs/xtensa-toolchain@v1.5
        with:
          default: true
          ldproxy: false
          cache: true

      - uses: Swatinem/rust-cache@v2

      - uses: extractions/setup-just@v2

      - name: Build ossm-alt
        run: just build-ossm-alt

      - name: Install espflash
        run: |
          curl -L https://github.com/esp-rs/espflash/releases/download/v3.3.0/espflash-x86_64-unknown-linux-gnu.zip -o espflash.zip
          unzip espflash.zip
          chmod +x espflash
          sudo mv espflash /usr/local/bin/

      - name: Create firmware binary
        run: espflash save-image --chip esp32s3 target/xtensa-esp32s3-none-elf/release/ossm-alt ossm-alt.bin

      - name: Create release
        run: gh release create "$TAG" ossm-alt.bin --title "$TAG" --notes "$NOTES"
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.tag.outputs.new_tag }}
          NOTES: ${{ steps.tag.outputs.changelog }}
