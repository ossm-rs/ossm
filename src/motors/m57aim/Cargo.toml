[package]
name = "m57aim-motor"
version = "0.1.0"
edition = "2024"

[dependencies]
sossm = { path = "../../lib/sossm" }
embassy-time = "0.5.0"
embedded-io-async = "0.7.0"
heapless = "0.9.2"
rmodbus = { version = "0.12.2", default-features = false, features = [
    "heapless",
] }
